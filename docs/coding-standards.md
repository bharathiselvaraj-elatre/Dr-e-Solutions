# Coding Standards

- Keep page interactions in page objects
- Keep test data in `test-data/`
- Prefer shared helpers only when they are used by active tests
- Remove placeholders instead of keeping empty scaffold coverage in the repo
